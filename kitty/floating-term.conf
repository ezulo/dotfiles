# Floating-term daemon configuration
# This kitty instance serves floating-term windows via remote control

allow_remote_control yes
listen_on unix:/run/user/1000/kitty-floating.sock

# Inherit main config
include kitty.conf

# Floating-term specific overrides
confirm_os_window_close 0
map ctrl+escape close_os_window
