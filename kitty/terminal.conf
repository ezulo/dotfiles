# Regular terminal daemon configuration
# This kitty instance serves main terminal windows via remote control

allow_remote_control yes
listen_on unix:/run/user/1000/kitty-terminal.sock

# Inherit main config
include kitty.conf
